project_id = "private"

[functions.mindbody-sync]
verify_jwt = false

[functions.mindbody-discover]
verify_jwt = false

[functions.mindbody-health]
verify_jwt = false

# Handler validates the access token with Auth and the protected admin role.
[functions.admin-room-details]
verify_jwt = false

# Checkout validates caller tokens with Auth. Webhook verifies Stripe signatures.
[functions.create-checkout]
verify_jwt = false

[functions.stripe-webhook]
verify_jwt = false

# Server-only cron authenticates with x-sync-key.
[functions.mindbody-client-sync]
verify_jwt = false

# Handler validates Auth tokens, roles and explicit CRM ownership.
[functions.client-accounts]
verify_jwt = false

# Validates either the scheduled sync key or a current admin session.
[functions.sleekflow-prospect-sync]
verify_jwt = false

# Auth verifies tokens; server-only RPC checks the stored admin role and session.
[functions.team-accounts]
verify_jwt = false

# Internal receipt email worker authenticates x-sync-key; downloads use create-checkout Auth.
[functions.official-receipts]
verify_jwt = false
